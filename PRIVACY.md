# 隐私政策 / Privacy Policy

生效日期：2026-07-23  
适用版本：知行 · AI认知监督台 v0.1.0

## 一、产品目的

本扩展的单一目的，是在用户设备本地帮助用户检查 AI 对话中的目标偏移、证据不足、反馈遗漏、重复表达和停止条件，并生成由用户自行决定是否使用的纠偏指令。

## 二、我们处理的数据

扩展可能在用户主动操作后处理：

- 用户明确选中的网页文字；
- 用户主动粘贴的文本；
- 用户填写的当前目标与完成标准；
- 扩展在本地生成的分析结果和检查点。

扩展不会读取或收集：

- 未被用户选中的网页内容；
- 浏览历史；
- Cookie、密码、付款信息或登录凭证；
- 摄像头、麦克风、位置或本地文件；
- 其他标签页或其他扩展的数据。

## 三、数据保存与传输

v0.1.0 不连接开发者服务器，不上传或传输用户内容。数据只保存在用户当前浏览器的 `chrome.storage.local` 中。

## 四、数据用途

数据只用于扩展明确展示的本地功能：分析偏航、区分疑似事实与假设、生成纠偏指令、保存检查点和导出记录。

## 五、共享、出售和广告

我们不出售、出租或共享用户数据；不使用用户数据进行个性化广告、再营销、用户画像或与扩展单一目的无关的市场研究。

## 六、人工访问

开发者与其他人员无法通过本扩展访问用户保存在本机的数据。本版本没有后台管理系统或内容查看接口。

## 七、权限

- `activeTab`：用户主动点击扩展时临时访问当前标签页。
- `scripting`：仅运行读取用户当前选中文字的脚本。
- `sidePanel`：显示浏览器侧边栏界面。
- `storage`：在本机保存用户主动输入的状态与检查点。

本扩展不声明 `host_permissions`。

## 八、用户控制

用户可以：

- 不授权读取任何网页内容，改用手动粘贴；
- 导出当前分析记录；
- 在扩展内删除全部本地数据；
- 通过Chrome扩展管理页撤销权限或卸载扩展。

## 九、有限使用承诺

本扩展对从Chrome API获得的信息的使用，仅限于提供和改进扩展明确、单一的用户可见功能。不会把这些信息用于广告、转售、信用评估、保险判断或与核心功能无关的用途。

## 十、变更

未来版本如增加网络传输、外部模型调用、账号同步或新的权限，必须在功能启用前明确告知用户，并更新本政策与Chrome Web Store隐私披露。默认不开启新增的数据传输能力。

## 十一、联系

发布前必须将本节替换为真实可用的支持邮箱和GitHub Issues地址。

---

## English Summary

Version 0.1.0 is local-first. It has no developer-operated backend, sends no user content over the network, reads only text explicitly selected by the user, stores state only in `chrome.storage.local`, does not sell or share data, and provides no automatic sending or execution capabilities.
